csv_dir: "dataset/10fold_csvs/" 

workers: 10

# 10-fold 用の成果物ルート（weights/, eval/ などの固定構成に展開されます）
tenfold_root: "artifacts/tenfold_integ"

search_space:
  model.Nx: [200, 300, 400, 500, 600]
  model.rho: [0.9]
  model.input_scale: [0.01, 0.005, 0.001]
