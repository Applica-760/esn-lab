train:
  csv_dir: "dataset/10fold_csvs/"
  workers: 10
  weight_dir: "artifacts/10fold_pred0"
  search_space:
    model.Nx: [200, 300, 400, 500, 600, 700, 800]
    model.density: [0.5]
    model.rho: [0.9]
    model.input_scale: [0.01]

eval:
  tenfold:
    csv_dir: "dataset/10fold_csvs/"
    weight_dir: "artifacts/10fold_pred0"
    parallel: true
    workers: 10
  summary:
    weight_dir: "artifacts/10fold_pred0"
    csv_name: "evaluation_results.csv"
    metric: "majority_acc"
    vary_param: "Nx"
    vary_values: [200, 300, 400, 500, 600, 700, 800]
    filters:
      density: 0.5
      input_scale: 0.01
      rho: 0.9
    fmt: ".3f"
    dpi: 150
