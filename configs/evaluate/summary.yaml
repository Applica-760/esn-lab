# Evaluate summary/plot configuration
weight_dir: "artifacts/10fold_weights"
csv_name: "evaluation_results.csv"

metric: "majority_acc"

vary_param: "Nx"
vary_values: [200, 300, 400, 500, 600, 700, 800]

filters:
  density: 0.5
  input_scale: 0.001
  rho: 0.9

fmt: ".3f"
dpi: 150
