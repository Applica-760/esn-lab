csv_dir: "dataset/10fold_csvs/" 

workers: 10

# 10-fold artifacts root (expands to weights/, eval/)
tenfold_root: "artifacts/tenfold_integ"

search_space:
  model.Nx: [200, 300, 400, 500, 600, 700, 800, 900]
  model.rho: [0.9]
  model.input_scale: [0.01, 0.005, 0.001]
