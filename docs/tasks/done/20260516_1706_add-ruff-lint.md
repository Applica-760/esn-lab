## 目的・前提・方針案
- Lintを導入し、import文や各種記法を統一する
- ツール: Ruff（lint + format + import整列を一括対応）
- 対象: `src/`, `projects/` 配下の全Pythonファイル（35ファイル）
- pre-commitフックも設定する

## 計画

### Phase 1: Ruff導入・設定
- [x] `uv add --dev ruff` で dev dependency に追加
- [x] `pyproject.toml` に `[tool.ruff]` セクションを追加（E, F, W, I ルール、line-length=100）

### Phase 2: 既存コードへの適用
- [x] `uv run ruff check --fix --unsafe-fixes .` で lint 自動修正（import整列・空白行）
- [x] F821バグ修正: `prediction_cli.py:461` の `analysis_dir` → `eval_dir`
- [x] E501残1件を手動修正（`plot_prediction.py:48` のf-stringを改行）

### Phase 3: pre-commitフック設定
- [x] `uv add --dev pre-commit` で追加
- [x] `.pre-commit-config.yaml` を作成（ruff check + ruff format）
- [x] `uv run pre-commit install` でフック有効化（`.git/hooks/pre-commit`）

## 実行ログ

## 結果
- Ruff (lint + format) + pre-commit フック導入完了
- import整列・空白行・スタイルの統一を自動適用（35ファイル対象）
- 副産物: F821バグ（未定義変数 `analysis_dir`）を検出・修正
- `git commit` 時に ruff が自動実行される
